<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
    <item android:id="@+id/clean_watchlist_menu"
          android:title="@string/clean_watchlist_menu"
          android:showAsAction="ifRoom"
          android:icon="@drawable/content_backspace"
            />
    -->
    <item android:id="@+id/clear_watchlist_menu"
          android:title="@string/clear_watchlist_menu"
          android:icon="@drawable/content_discard"
          android:showAsAction="ifRoom"
            />
</menu>
