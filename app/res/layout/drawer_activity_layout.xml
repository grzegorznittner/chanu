<?xml version="1.0" encoding="utf-8"?>

<android.support.v4.widget.DrawerLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/drawer_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    <!-- The main content view -->
    <FrameLayout
            android:id="@+id/content_frame"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
    <!-- The navigation drawer -->
    <ListView android:id="@+id/left_drawer"
              android:layout_width="@dimen/Drawer.layout_width"
              android:layout_height="match_parent"
              android:layout_gravity="start"
              android:paddingTop="@dimen/Drawer.paddingTop"
              android:choiceMode="singleChoice"
              android:dividerHeight="2dp"
              android:divider="@color/PaletteTransparent"
              android:background="?themedDrawerBackground"/>
</android.support.v4.widget.DrawerLayout>

        <!--
              android:divider="@drawable/drawer_divider"
              android:dividerHeight="1dp"

              android:divider="@color/PaletteDarkGray"
              android:dividerHeight="1dp"
        android:divider="@android:color/transparent"
                      android:dividerHeight="0dp"
-->
